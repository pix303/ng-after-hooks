import { DateViewerDirective } from './date-viewer.directive';

describe('DateViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new DateViewerDirective();
    expect(directive).toBeTruthy();
  });
});
